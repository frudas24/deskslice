# TODO — Codex Remote Panel (WebRTC + Touch Control + Fullscreen Presetup + Password)

## Objetivo

Remote desktop tuneado (LAN) para operar **la interfaz real del panel Codex dentro de VS Code** desde el celular:

* **Video:** WebRTC (baja latencia)
* **Control:** touch → mouse/keyboard (tap + drag-scroll + typing)
* **Presetup fullscreen:** seleccionar monitor + trazar rectángulos (plugin/chat/scroll)
* **Run cropped:** stream recortado solo al panel Codex
* **Seguridad leve:** UI pide **password** definido en `.env`
* **Sin persistencia:** no se guardan chats ni logs, solo calib y estado mínimo.

---

## 0) Requerimientos

### Funcionales

* [ ] Servir una UI web (mobile-first) accesible en LAN: `http://PC:8787`.
* [ ] La UI debe pedir password antes de habilitar:

  * WebRTC video
  * WS signaling
  * WS control
* [ ] Modo `Presetup (Fullscreen)`:

  * [ ] stream de **pantalla completa** del monitor seleccionado
  * [ ] UI permite trazar:

    * [ ] `plugin_rect` (zona del panel Codex en VS Code)
    * [ ] `chat_rect` (área del input)
    * [ ] `scroll_rect` (área scrolleable de mensajes)
  * [ ] botón `Restart Presetup` para recalibrar y/o cambiar monitor.
* [ ] Modo `Run (Cropped)`:

  * [ ] stream recortado solo a `plugin_rect`
  * [ ] tap/click y drag-scroll afectan Windows en esa zona
  * [ ] typing desde `<textarea>` del móvil → host hace click en `chat_rect`, escribe texto, Enter
* [ ] UI debe permitir seleccionar monitor en presetup:

  * [ ] dropdown “Monitor 1..N”
  * [ ] si te equivocas → `Restart Presetup` y cambias monitor.
* [ ] `Input Enabled` kill-switch:

  * [ ] OFF: sigue video, pero se ignora todo input (mouse/keyboard)

### No funcionales

* [ ] MVP: 1 viewer activo (si entra otro, se rechaza o reemplaza: definir e implementar).
* [ ] “No magia”: sin auto-validación; tú calibras manualmente y listo.
* [ ] Windows 11.
* [ ] Layout por dominios: `./cmd` cableado, `./internal` lógica.

---

## 1) Estructura del repo (entregable)

* [ ] Crear:

```
./cmd/
  codex_remote/
    main.go

./internal/
  config/
    config.go
  app/
    app.go
    http_server.go
    ws_routes.go
  session/
    session.go
  calib/
    model.go
    store.go
  monitor/
    monitor_windows.go
  ffmpeg/
    runner.go
    presets.go
  webrtc/
    publisher.go
    rtp_ingest.go
  signaling/
    ws_signaling.go
    protocol.go
  control/
    ws_control.go
    protocol.go
    mapper.go
    gestures.go
    actions.go
  wininput/
    injector.go
    sendinput_windows.go
    mouse.go
    keyboard.go
  web/
    static/
      index.html
      app.js
      styles.css
  testutil/
    fake_injector.go

./data/
  .env        (runtime)
  calib.json  (runtime)
```

* [ ] `.gitignore`:

  * [ ] `./data/*` (ignorar todo en data)

---

## 2) Dominio: config (`./internal/config`)

### Requerimientos

* [ ] Leer `.env` y env vars.
* [ ] Password obligatorio.

### Entregables

* [ ] `Config`:

```go
type Config struct {
  ListenAddr   string // "0.0.0.0:8787"
  UIPassword   string // env: UI_PASSWORD (required)
  DataDir      string // "./data"
  CalibPath    string // "./data/calib.json"
  FFmpegPath   string // "ffmpeg" o ruta
  FPS          int    // 30
  BitrateKbps  int    // 6000
  MonitorIndex int    // 1
}
```

* [ ] `Load() (Config, error)`:

  * [ ] si `UI_PASSWORD` vacío → error explícito.

---

## 3) Dominio: session (`./internal/session`)

### Requerimientos

* [ ] Estado runtime global:

  * auth
  * inputEnabled
  * mode presetup|run
  * monitorIdx
  * calib
  * punteros/refs a pipeline actual (opcional, pero usualmente se maneja desde app)

### Entregables

* [ ] `Session` thread-safe:

  * [ ] `Authenticate(pass string) bool`
  * [ ] `Logout()`
  * [ ] `IsAuthenticated() bool`
  * [ ] `SetInputEnabled(bool)`
  * [ ] `InputEnabled() bool`
  * [ ] `SetMode(mode string)`
  * [ ] `Mode() string`
  * [ ] `SetMonitor(idx int)`
  * [ ] `Monitor() int`
  * [ ] `SetCalib(c calib.Calib)`
  * [ ] `GetCalib() calib.Calib`
  * [ ] `Snapshot()` (para UI / handlers)

---

## 4) Dominio: monitor (`./internal/monitor`)

### Requerimientos

* [ ] Enumerar monitores con WinAPI.

### Entregables

* [ ] `type Monitor struct { Index int; X,Y,W,H int; Primary bool }`
* [ ] `ListMonitors() ([]Monitor, error)`
* [ ] `GetMonitorByIndex(list []Monitor, idx int) (Monitor, bool)`
* [ ] API: `GET /api/monitors`

---

## 5) Dominio: calib (`./internal/calib`)

### Requerimientos

* [ ] Guardar:

  * `PluginAbs` (coords absolutas en monitor)
  * `ChatRel` (coords dentro de plugin)
  * `ScrollRel` (coords dentro de plugin)
  * `MonitorIndex` asociado
* [ ] Save/Load JSON.

### Entregables

* [ ] Tipos:

```go
type Rect struct{ X, Y, W, H int }
type Calib struct {
  MonitorIndex int
  PluginAbs    Rect
  ChatRel      Rect
  ScrollRel    Rect
}
```

* [ ] Helpers:

  * [ ] `Normalize(r Rect) Rect`
  * [ ] `Contains(r Rect, x, y int) bool`
* [ ] `Load(path) (Calib, error)` (si no existe → calib “vacía”)
* [ ] `Save(path, c Calib) error`

---

## 6) Dominio: ffmpeg (`./internal/ffmpeg`)

### Requerimientos

* [ ] Dos modos:

  * presetup: monitor full
  * run: monitor full + crop a PluginAbs
* [ ] Salida a RTP H264 por UDP `127.0.0.1:<port>` (para Pion).
* [ ] Restart atómico en cambios: mode/monitor/plugin rect.

### Entregables

* [ ] `Runner`:

  * [ ] `StartPresetup(m monitor.Monitor, opts) (rtpPort int, stop func() error, err error)`
  * [ ] `StartRun(m monitor.Monitor, plugin calib.Rect, opts) (rtpPort int, stop func() error, err error)`
  * [ ] `Stop() error`
  * [ ] `Restart(...)` (stop anterior + start nuevo)
* [ ] `presets.go` con builders:

  * [ ] command presetup
  * [ ] command run (incluye `crop=w:h:x:y`)
* [ ] Captura preferida: `d3d11grab` (fallback `gdigrab` si falla).

---

## 7) Dominio: webrtc (`./internal/webrtc`)

### Requerimientos

* [ ] Pion WebRTC, track H264.
* [ ] UDP listener RTP local reenvía paquetes a `TrackLocalStaticRTP`.

### Entregables

* [ ] `Publisher`:

  * [ ] `NewPublisher()`
  * [ ] `NewPeer() (*webrtc.PeerConnection, error)`
  * [ ] `Track() (*webrtc.TrackLocalStaticRTP, error)`
  * [ ] `AttachRTP(port int) error`
  * [ ] `StartForwarding()`
  * [ ] `StopForwarding()`
  * [ ] `ClosePeer()`
* [ ] `rtp_ingest.go`:

  * [ ] UDP loop read→WriteRTP

---

## 8) Dominio: signaling (`./internal/signaling`)

### Requerimientos

* [ ] WebSocket signaling (offer/answer/ice)
* [ ] Requiere sesión autenticada
* [ ] 1 viewer activo

### Entregables

* [ ] WS `/ws/signal`
* [ ] Mensajes:

```json
{ "t":"offer", "sdp":"..." }
{ "t":"answer", "sdp":"..." }
{ "t":"ice", "candidate":{...} }
{ "t":"restart" }
```

* [ ] `ws_signaling.go`:

  * [ ] create/close peerconnection
  * [ ] ICE exchange
  * [ ] enviar `restart` cuando pipeline reinicia

---

## 9) Dominio: wininput (`./internal/wininput`)

### Requerimientos

* [ ] Inyectar mouse/keyboard con WinAPI.
* [ ] Debe ser mockeable para unit tests.

### Entregables

* [ ] Interfaz `Injector` (`injector.go`):

```go
type Injector interface {
  MoveAbs(x, y int) error
  LeftDown() error
  LeftUp() error
  ClickAt(x, y int) error
  TypeUnicode(text string) error
  Enter() error
  Wheel(delta int) error // opcional
}
```

* [ ] Implementación real `WinInjector` (SendInput) en:

  * [ ] `sendinput_windows.go`
  * [ ] `mouse.go`
  * [ ] `keyboard.go`

---

## 10) Dominio: control (`./internal/control`)

### Requerimientos

* [ ] WS `/ws/control` (auth requerido)
* [ ] Kill-switch respeta inputEnabled
* [ ] Mapping coords:

  * presetup: norm → monitor abs
  * run: norm → plugin abs
* [ ] Drag-scroll default (si down inicia en scroll_rel)
* [ ] Typing:

  * [ ] `type{text}` seguido de `enter`:

    * click en chat (centro de ChatRel)
    * type unicode
    * enter

### Entregables

#### 10.1 Protocol (`protocol.go`)

* [ ] Mensajes:

```json
{ "t":"down", "id":1, "x":0.5, "y":0.2 }
{ "t":"move", "id":1, "x":0.5, "y":0.25 }
{ "t":"up",   "id":1, "x":0.5, "y":0.25 }
{ "t":"type", "text":"..." }
{ "t":"enter" }

{ "t":"setMode", "mode":"presetup" }
{ "t":"setMode", "mode":"run" }
{ "t":"setMonitor", "idx":2 }
{ "t":"restartPresetup" }

{ "t":"calibRect", "step":"plugin", "rect":{"x":..,"y":..,"w":..,"h":..} }
{ "t":"calibRect", "step":"chat",   "rect":{"x":..,"y":..,"w":..,"h":..} }
{ "t":"calibRect", "step":"scroll", "rect":{"x":..,"y":..,"w":..,"h":..} }

{ "t":"inputEnabled", "enabled":true }
```

#### 10.2 Mapping (`mapper.go`)

* [ ] Funciones:

  * [ ] `NormToAbsPresetup(xn,yn float64, m monitor.Monitor) (x,y int)`
  * [ ] `NormToAbsRun(xn,yn float64, plugin calib.Rect) (x,y int)`
  * [ ] clamp de xn/yn a `[0..1]`

#### 10.3 Gestures (lógica pura) (`actions.go`, `gestures.go`)

* [ ] Definir acciones:

```go
type ActionType string
const (
  ActMove ActionType = "move"
  ActLeftDown ActionType = "left_down"
  ActLeftUp ActionType = "left_up"
  ActClick ActionType = "click"
  ActType ActionType = "type"
  ActEnter ActionType = "enter"
)
type Action struct { Type ActionType; X,Y int; Text string }
```

* [ ] State machine:

  * `down`:

    * en run: calcular frameX/Y dentro del plugin
    * si cae en ScrollRel → iniciar drag (LeftDown)
    * else → click (ClickAt)
  * `move`:

    * solo si drag activo y pointerId coincide
    * throttle 30–60 Hz + ignore micro deltas
  * `up`:

    * si drag activo → LeftUp
* [ ] Kill-switch: si inputDisabled → devolver `[]Action{}` en down/move/up/type/enter.

#### 10.4 WS handler (`ws_control.go`)

* [ ] Traducir mensajes → `[]Action` → ejecutar via `wininput.Injector`
* [ ] En cambios de monitor/mode/calib → notificar a `app` para restart pipeline (callback o channel).

---

## 11) Dominio: app (`./internal/app`)

### Requerimientos

* [ ] HTTP + Static UI
* [ ] Login endpoint y gating
* [ ] Restart pipeline y notify a signaling (`restart`)
* [ ] MVP 1 viewer: decidir política:

  * [ ] A) rechazar segunda conexión
  * [ ] B) expulsar anterior

### Entregables

#### 11.1 HTTP (`http_server.go`)

* [ ] Endpoints:

  * [ ] `POST /login` `{password}`

    * 200 OK si match `.env`
    * 401 si no
  * [ ] `POST /logout` (opcional)
  * [ ] `GET /api/monitors`
  * [ ] `GET /api/state` (mode, monitor, inputEnabled, calib present/step completeness)
  * [ ] `/` static

#### 11.2 WS routing (`ws_routes.go`)

* [ ] `/ws/signal`:

  * cerrar si `!session.IsAuthenticated()`
* [ ] `/ws/control`:

  * cerrar si `!session.IsAuthenticated()`

#### 11.3 Pipeline manager (`app.go`)

* [ ] `Start()`:

  * enum monitores
  * load calib (si existe)
  * set modo presetup
  * start presetup pipeline
* [ ] `RestartPipeline(reason)`:

  * stop ffmpeg
  * stop webrtc forwarding
  * start ffmpeg con:

    * presetup: monitor full
    * run: crop plugin
  * attach RTP port to publisher
  * notify signaling client `{t:"restart"}`

---

## 12) UI (`./internal/web/static`)

### Login

* [ ] password input + submit → `/login`
* [ ] solo si OK inicializa:

  * signaling WS
  * webrtc negotiation
  * control WS

### Operación

* [ ] Video WebRTC
* [ ] Top bar:

  * mode selector (Presetup/Run)
  * monitor dropdown
  * Restart presetup
  * InputEnabled toggle
* [ ] Calibración:

  * botones: Set Plugin / Set Chat / Set Scroll
  * overlay canvas para trazar rect
  * botón Save (envía `calibRect`)
* [ ] Typing:

  * `<textarea>` + botón Send:

    * manda `type{text}` + `enter`
* [ ] Gestos:

  * tap: down/up
  * drag: down/move/up (1 dedo) (en run será drag-scroll si comenzó en scroll)

---

# Cableado — `./cmd/codex_remote/main.go` (solo wiring)

* [ ] `Load config`
* [ ] `List monitors`
* [ ] `Load calib`
* [ ] Init `Session`
* [ ] Init `FFmpeg Runner`
* [ ] Init `WebRTC Publisher`
* [ ] Init `WinInjector`
* [ ] Init `Signaling Server`
* [ ] Init `Control Server` (con injector mockable)
* [ ] Init `App` (con callbacks para restart)
* [ ] Start HTTP server
* [ ] Start pipeline presetup
* [ ] Graceful shutdown

---

# TODO EXTRA — Unit Tests (anti-regresión)

## Objetivo

Unit tests deterministas para la lógica crítica:

* calib rect ops
* mapping coords
* gestures state machine (drag-scroll)
* kill switch
* session auth
* protocol json decode

### 1) Infra de tests (`./internal/testutil`)

* [ ] `fake_injector.go`: implementa `wininput.Injector`, guarda calls en slice:

  * `[]Call{ Name, X,Y, Text }`

### 2) Tests: calib (`./internal/calib`)

* [ ] `model_test.go`

  * [ ] `TestNormalizeRect_Positive`
  * [ ] `TestNormalizeRect_NegativeDims`
  * [ ] `TestContains_Inside`
  * [ ] `TestContains_Edges` (definir borde inclusive)
  * [ ] `TestContains_Outside`
* [ ] `store_test.go`

  * [ ] `TestSaveLoad_RoundTrip`
  * [ ] `TestLoad_MissingFile_ReturnsEmpty` (según diseño)

### 3) Tests: mapper (`./internal/control/mapper_test.go`)

* [ ] `TestNormToAbsPresetup_TopLeft`
* [ ] `TestNormToAbsPresetup_Center`
* [ ] `TestNormToAbsPresetup_BottomRight` (documentar criterio 1.0)
* [ ] `TestNormToAbsRun_TopLeft`
* [ ] `TestNormToAbsRun_Center`
* [ ] `TestNormToAbsRun_BottomRight`
* [ ] `TestNormToAbs_ClampOutOfRange`

### 4) Tests: gestures/actions (`./internal/control/gestures_test.go`)

* [ ] `TestDragScroll_StartsOnlyInsideScrollRel`
* [ ] `TestDragScroll_MoveOnlyWhenActiveAndSamePointer`
* [ ] `TestDragScroll_UpStopsAndEmitsLeftUp`
* [ ] `TestTapOutsideScroll_EmitsClick`
* [ ] `TestInputDisabled_NoActions`
* [ ] `TestTypeEnter_EmitsClickTypeEnterSequence` (si lo defines así)

### 5) Tests: session (`./internal/session/session_test.go`)

* [ ] `TestAuthenticate_Success`
* [ ] `TestAuthenticate_Fail`
* [ ] `TestLogout`
* [ ] `TestInputEnabled_Toggle`
* [ ] `TestSnapshot`

### 6) Tests: protocol JSON

* [ ] `./internal/control/protocol_test.go`

  * [ ] decode down/move/up/type/enter
* [ ] `./internal/signaling/protocol_test.go`

  * [ ] decode offer/ice

### 7) Smoke checklist (manual) en README

* [ ] Login OK/FAIL
* [ ] Presetup fullscreen + monitor correcto
* [ ] Calibración plugin/chat/scroll
* [ ] Run cropped OK
* [ ] Tap focus chat
* [ ] Typing + Enter funciona
* [ ] Drag-scroll usable
* [ ] InputEnabled OFF bloquea control
* [ ] Restart presetup + cambio monitor

---
# recomendación para arrancar sin fricción

1. **Empieza por lo 100% testeable**

   * `calib`
   * `mapper`
   * `gestures` + `actions`
   * `session`
   * unit tests
     Esto te deja la lógica crítica blindada desde el día 1.

2. **Luego el pipeline “visible”**

   * `monitor` (enum)
   * `ffmpeg` presetup/run
   * `webrtc` RTP ingest

3. **Al final UX**

   * UI fullscreen calibrator
   * WS control + signaling
   * typing / drag polish

